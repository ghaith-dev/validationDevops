version: '3.8'
services:
  BACKEND:
    image: ghaith19/devops
    volumes:
      - ./app:/app
      - .m2:/root/.m2
    ports:
      - 8089:8089
    working_dir: /app
    depends_on:
      - MYSQL-DATABASE
  MYSQL-DATABASE:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_USER: myuser
      MYSQL_PASSWORD: mypassword
    ports:
      - 3306:3306
    volumes:
      - ./data:/var/lib/mysql